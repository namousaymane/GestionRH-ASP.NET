@model GestionRH.Models.Employe

@{
    ViewData["Title"] = "Supprimer";
}

<div class="container mt-4">
    <h1 class="text-danger">Supprimer un employé</h1>
    <h3>Êtes-vous sûr de vouloir supprimer cet employé ?</h3>
    <div class="alert alert-warning">
        Attention : Cette action est irréversible et supprimera aussi l'historique de ses congés et paies.
    </div>

    <div class="card shadow-sm mt-3">
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">Nom complet</dt>
                <dd class="col-sm-9">@Model.NomComplet</dd>

                <dt class="col-sm-3">Email</dt>
                <dd class="col-sm-9">@Model.Email</dd>

                <dt class="col-sm-3">Poste</dt>
                <dd class="col-sm-9">@Model.Poste</dd>
            </dl>

            <form asp-action="Delete">
                <input type="hidden" asp-for="Id" />
                <input type="submit" value="Confirmer la suppression" class="btn btn-danger" />
                <a asp-action="Index" class="btn btn-secondary">Annuler</a>
            </form>
        </div>
    </div>
</div>